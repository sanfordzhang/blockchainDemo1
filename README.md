# SimpleToken DApp

一个基于Sepolia测试网络的简单代币DApp，使用Solidity智能合约和Ethers.js构建。

## 功能特性

- 🪙 **ERC20代币合约** - 完整的代币标准实现
- 🔌 **MetaMask集成** - 通过MetaMask连接Sepolia测试网络
- 📜 **合约部署** - 直接从前端部署智能合约
- 💱 **代币转账** - 转账给其他地址
- 💰 **余额查询** - 查询账户余额
- 🎨 **美观界面** - 现代化的用户界面

## 技术栈

- **智能合约**: Solidity 0.8.19
- **前端框架**: HTML5 + JavaScript (ES6+)
- **区块链库**: Ethers.js v6
- **构建工具**: Vite
- **开发框架**: Hardhat

## 安装步骤

### 1. 克隆项目

```bash
git clone <repository-url>
cd demo1
```

### 2. 安装依赖

```bash
npm install
```

### 3. 编译合约（生成ABI和字节码）

```bash
npx hardhat compile
node compile.js
```

这将生成以下文件：
- `src/contractABI.json` - 合约ABI
- `src/contractBytecode.txt` - 合约字节码

### 4. 启动开发服务器

```bash
npm run dev
```

应用将在 `http://localhost:3000` 打开。

## 环境变量配置（可选）

### 关于 `hardhat.config.js` 中的环境变量

项目中的 `hardhat.config.js` 配置了两个环境变量：

```javascript
url: process.env.SEPOLIA_RPC_URL || "",
accounts: process.env.PRIVATE_KEY ? [process.env.PRIVATE_KEY] : []
```

**重要说明：**

- ✅ **当前DApp通过前端操作，无需配置这些环境变量**
- ⚠️ 这些变量仅在使用Hardhat脚本部署合约时需要
- ✅ 前端通过MetaMask连接，使用MetaMask的签名，不需要私钥
- 🔐 环境变量从项目的 `.env` 文件中读取

### 何时需要配置环境变量？

**需要配置的情况：**
- 使用Hardhat脚本自动化部署合约
- 运行测试脚本
- CI/CD自动化部署流程

**不需要配置的情况：**
- 通过前端界面部署合约（本项目的使用方式）
- 只使用MetaMask交互
- 运行 `npm run dev` 启动开发服务器

### 如何配置（如果需要）

1. **创建 `.env` 文件**

参考 `.env.example` 文件创建配置：

```bash
# 复制示例文件
cp .env.example .env
```

2. **编辑 `.env` 文件**

```env
# Sepolia RPC URL（使用公共RPC或从Infura/Alchemy获取）
SEPOLIA_RPC_URL=https://rpc.sepolia.org

# MetaMask账户私钥（仅用于测试，不要使用主网私钥！）
# 获取方式：MetaMask -> 账户详情 -> 导出私钥
PRIVATE_KEY=your_private_key_here
```

3. **安全提示**

⚠️ 永远不要将包含真实私钥的 `.env` 文件提交到Git
✅ `.env` 文件已在 `.gitignore` 中自动排除

详细配置说明请参考 [ENV_SETUP.md](./ENV_SETUP.md)

### 4. 启动开发服务器

```bash
npm run dev
```

应用将在 `http://localhost:3000` 打开。

## 使用说明

### 前置条件

1. **安装MetaMask钱包扩展**
   - 访问 [MetaMask官网](https://metamask.io/) 下载安装

2. **配置Sepolia测试网络**
   - 打开MetaMask
   - 在网络下拉菜单中选择"添加网络"
   - 选择Sepolia测试网络
   - 或手动配置：
     - 网络名称: Sepolia
     - RPC URL: `https://sepolia.infura.io/v3/YOUR_INFURA_KEY` 或其他公共RPC
     - Chain ID: 11155111
     - 货币符号: ETH

3. **获取测试ETH**
   - 访问 [Sepolia Faucet](https://sepoliafaucet.com/)
   - 或使用 [Alchemy Faucet](https://sepoliafaucet.com/)
   - 输入你的钱包地址领取测试ETH

### 使用流程

1. **连接钱包**
   - 点击"连接MetaMask钱包"按钮
   - 在MetaMask中授权连接
   - 确保已连接到Sepolia网络

2. **部署合约**
   - 填写代币信息：
     - 代币名称（如：My Token）
     - 代币符号（如：MTK）
     - 初始供应量（如：1000000）
   - 点击"部署智能合约"按钮
   - 在MetaMask中确认交易
   - 等待部署完成（约15-30秒）

3. **查询余额**
   - 部署成功后，点击"查询余额"按钮
   - 查看您的代币余额

4. **转账**
   - 输入接收地址
   - 输入转账数量
   - 点击"转账"按钮
   - 在MetaMask中确认交易
   - 等待交易确认

## 合约功能

### SimpleToken合约包含以下功能：

- `transfer(address _to, uint256 _value)` - 转账
- `approve(address _spender, uint256 _value)` - 授权
- `transferFrom(address _from, address _to, uint256 _value)` - 从授权账户转账
- `getBalance(address _address)` - 查询余额

### 只读属性：

- `name` - 代币名称
- `symbol` - 代币符号
- `decimals` - 小数位数（18）
- `totalSupply` - 总供应量
- `balanceOf` - 余额映射
- `allowance` - 授权映射

## 错误处理

应用包含以下错误处理机制：

- MetaMask未安装提示
- 网络连接错误处理
- 交易失败提示
- 输入验证
- 账户余额检查

## 开发命令

```bash
# 编译合约
npx hardhat compile

# 运行编译脚本
node compile.js

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 预览生产构建
npm run preview
```

## 注意事项

1. **测试网络** - 此项目仅在Sepolia测试网络上运行
2. **测试代币** - 这些代币仅在测试网络有效，没有实际价值
3. **Gas费用** - 部署和调用合约需要消耗Sepolia测试ETH
4. **安全性** - 此为学习演示项目，不建议用于生产环境

## 测试网络RPC节点

以下是一些免费的Sepolia RPC端点：

- Infura: 需要注册获取API Key
- Alchemy: 需要注册获取API Key
- QuickNode: 需要注册获取API Key
- 公共RPC: `https://rpc.sepolia.org`

## 测试ETH水龙头

- [Sepolia Faucet](https://sepoliafaucet.com/)
- [Alchemy Sepolia Faucet](https://sepoliafaucet.com/)
- [Google Cloud Sepolia Faucet](https://cloud.google.com/application/web3/faucet/ethereum/sepolia)

## 许可证

MIT License

## 贡献

欢迎提交Issue和Pull Request！
