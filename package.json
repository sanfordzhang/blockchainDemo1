{
  "name": "simple-dapp",
  "version": "1.0.0",
  "description": "A simple DApp with token contract",
  "main": "index.html",
  "scripts": {
    "dev": "npx vite",
    "build": "npx vite build",
    "preview": "npx vite preview",
    "compile": "npx hardhat compile && node compile.js"
  },
  "dependencies": {
    "ethers": "^6.9.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0",
    "vite": "^5.0.8"
  }
}
